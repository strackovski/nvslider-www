define(['./baseModel'], function (BaseModel) {

    var model1 = new BaseModel('Make slidable grid views from list items with nv/Slider.', 'Easy to use slider plug-in for jQuery.');

    return model1;
});